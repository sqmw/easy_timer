import 'package:flutter/material.dart';

class NumberStylePage extends StatelessWidget {
  const NumberStylePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('数字倒计时风格页面'),
    );
  }
}