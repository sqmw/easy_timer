import 'package:flutter/material.dart';

class ChartStylePage extends StatelessWidget {
  const ChartStylePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('图表倒计时风格页面'),
    );
  }
}